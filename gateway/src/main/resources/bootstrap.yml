spring:
  application:
    name: gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev
      label: master
#server:
#  port: 80
#eureka:
#  client:
#    service-url:
#      defaultZone: http://peer1:1111/eureka/,http://peer2:1112/eureka/
#zuul:
#  add-host-header: true
